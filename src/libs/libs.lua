local libs = {
    spawn_objects = {
        object = {
            animation = require('src.libs.spawn_obj_animation'),
            swipe = require('src.libs.spawn_obj_swipe'),
        }
    }
}

return libs
